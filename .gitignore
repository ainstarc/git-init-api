# Node.js and TypeScript .gitignore

# Logs
# Log files are typically machine-specific and not needed in version control.
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
# These files are often generated at runtime and can vary between environments.
pids
*.pid
*.seed
*.pid.lock

# Dependency directories
# The node_modules directory contains all your project's dependencies.
# It can be very large and should be generated by running `npm install` or `yarn`.
node_modules/

# TypeScript compiled output
# These directories contain the compiled JavaScript from your TypeScript source.
dist/
build/
.out/

# Test coverage
# Coverage reports are generated when running tests and shouldn't be committed.
coverage/
lcov.info
.nyc_output/

# Environment variables
# Never commit .env files as they may contain sensitive API keys and secrets.
.env
.env.local
.env.*.local

# IDE and editor files
.vscode/
.idea/
.history/

# OS-generated files
.DS_Store
Thumbs.db
